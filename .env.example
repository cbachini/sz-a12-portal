# Portal A12 — Variáveis de ambiente LOCAL
# Copie para .env e preencha antes de rodar docker compose up
# NUNCA commite o arquivo .env

# ---------------------------------------------------------------
# Banco de dados (MySQL local)
# ---------------------------------------------------------------
DB_NAME=a12_local
DB_USER=a12
DB_PASSWORD=troque_esta_senha
DB_ROOT_PASSWORD=troque_root_senha

# ---------------------------------------------------------------
# WordPress
# ---------------------------------------------------------------
WP_TABLE_PREFIX=wp_

# Gere as chaves em: https://api.wordpress.org/secret-key/1.1/salt/
WP_AUTH_KEY=coloque_chave_aqui
WP_SECURE_AUTH_KEY=coloque_chave_aqui
WP_LOGGED_IN_KEY=coloque_chave_aqui
WP_NONCE_KEY=coloque_chave_aqui
WP_AUTH_SALT=coloque_chave_aqui
WP_SECURE_AUTH_SALT=coloque_chave_aqui
WP_LOGGED_IN_SALT=coloque_chave_aqui
WP_NONCE_SALT=coloque_chave_aqui

# ---------------------------------------------------------------
# Indexação por robôs de busca
# true  = bloqueia indexação (padrão para ambientes de teste)
# false = permite indexação (produção)
# ---------------------------------------------------------------
WP_NOINDEX=true

# ---------------------------------------------------------------
# Ambiente / Storage (usado em DEV, STAGE e PROD na AWS)
# local | dev | stage | prod
# ---------------------------------------------------------------
A12_ENV=local

# Uploads em S3 (não usado em local)
S3_UPLOADS_BUCKET=
S3_UPLOADS_REGION=sa-east-1
S3_UPLOADS_BUCKET_URL=

# Redis (status atual): não configurado no código da aplicação.
# Se habilitar no futuro, definir variáveis conforme plugin/estratégia adotada.

# ---------------------------------------------------------------
# Portas locais (evite conflito com outros serviços)
# ---------------------------------------------------------------
WP_PORT=8080
MYSQL_PORT=3306
PMA_PORT=8081
